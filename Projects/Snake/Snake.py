import pygame

#initializing pygame
pygame.init()

#sets up a display mode that is on the parameter x 400 and y 300
dis=pygame.display.set_mode((400,300))

#updates the screen display
pygame.display.update()

#uninitializes everything
pygame.quit()
quit()
